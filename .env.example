# Keycloak admin (부트스트랩 전용)
KC_ADMIN=admin
KC_ADMIN_PASSWORD=change-me-admin

# zot <-> Keycloak 연동용 클라이언트 시크릿
# configure-realm.sh가 생성한 값을 bootstrap.sh가 자동으로 여기에 써넣습니다.
ZOT_OIDC_CLIENT_SECRET=replace-after-realm-setup

# CI(htpasswd) 계정 비밀번호 (bootstrap.sh가 사용)
CI_PASSWORD=change-me-ci
